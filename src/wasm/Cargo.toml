[package]
name = "qeyloop-dsp"
version = "1.0.0"
edition = "2021"
description = "Low-latency DSP engine for Qeyloop launchpad"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.89"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
